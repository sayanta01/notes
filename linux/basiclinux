- use 'quote' while creating something which have spaces or use\ backslash 
cd -                                         - go back
mkdir -p code/python/tutorial/hellow-world   - parent directory
mkdir -p linux/{1..99}/{a..z}/{sayanta,singh}
rm -rf *.mkv                                 - verbose delete

timeout 1(s,m,h) htop                        - run command with time
id                                           - check user id

-helping command-
use \ run commands thats have alias       - \vim #ihave nvim as nvim in alias
sudoedit /etc/hosts                          - edit root files with nvim config

