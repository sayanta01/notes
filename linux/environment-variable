what is - location of storing a value

# dynamic values
# exist in every operating system
# can be created edited saves and deleted
# gives info bout the system behavior
# change the way software programs behave

env    # display all the evironment variables --use grep to sort
set    # split things up based on numbers
set $(ifconfig | grep inet | head -n 1)  --run echo $nm
declare  
printenv    # like echo $env

$UID     # user's unique ID
$LANG    # user lang
$USER    # name of user
$HOME    # path of user's home dir
$TERM    # default terminal emulator
$SHELL   # shell used by the user
$EDITOR  # default editor
$PATH    # path
export PATH=$PATH:$HOME/.local/bin




